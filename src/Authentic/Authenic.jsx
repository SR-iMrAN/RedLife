import React from 'react';
import { Outlet } from 'react-router-dom';

const Authenic = () => {
    return (
        <div>
           
            <Outlet />
        </div>
    );
};

export default Authenic;
